# 怪獸放電場｜線上預約系統 — 功能對照進度

依「預約系統需求規格」與業主後續回覆的須知文件，逐項對照目前雛形（`c:\Users\sean.wang.WENTAI\my project\kid_exhibiton`）的完成狀況。

圖例：✅ 已完成　⚠️ 部分完成（有做但不完整）　⛔ 尚未開始

---

## 業主須知回覆帶來的規格調整（已依此更新程式碼）

| 項目 | 業主決定 | 狀態 |
|---|---|---|
| 4-1 每日限預約一次 | 整個取消，全面開放 | ✅ 已移除資料庫去重限制與相關錯誤處理 |
| 4-2 場次關閉時機 | 場次開始前 15 分鐘自動停止該場次預約 | ✅ 已實作並實測 |
| 4-2 展期結束後 | 頁面全部關閉，顯示「活動已結束」 | ✅ 已實作 |
| 4-3 布告欄文字 | 後台要能自己編輯 | ✅ 已新增「活動設定」頁面 |
| 4-5 名額調整 | 傾向不調整，固定人數就好 | 功能維持不變（已做好、已測試，業主表示不會用到調降情境，無需額外處理） |
| 4-6 到場狀態 | 取消，現場改用人工計數器 | ✅ 已移除到場狀態相關功能 |
| 5-1 個資同意文字 | 確認沿用 | ✅ 原本使用的文字剛好完全對得上，未變更 |
| 5-2 送出前防呆 | 新增「確認資料」頁面，顯示全部填寫內容＋按鈕才送出 | ✅ 已實作並測試 |

## 介面視覺（依業主提供的「Monster Play System」設計系統套用）

業主提供了完整的設計規格包（`ui/` 資料夾，色票、圓角、硬陰影、字體、按鈕、月曆、場次卡、表單、卡片、憑證、分頁圖示、頁首彈窗全部都有明確定義）。這輪已把**現有畫面**（前台四步驟流程、後台四個頁面、錯誤訊息、彈窗）全部套上這套視覺，範圍不含新頁面／新機制：

- ✅ 色票、圓角、陰影、字體（Bricolage Grotesque + Noto Sans TC 自架字型，含兩階段中文載入）已設定進 `tailwind.config.ts` / `globals.css`
- ✅ 按鈕、月曆六種狀態、場次卡五種狀態、表單三種狀態、卡片組（警示／資訊／成功）都已套用
- ✅ 頁首（含跑馬燈）、彈窗（取代原本的行內錯誤文字，例如額滿時改跳出彈窗）已套用
- ✅ 預約成功頁改成 MONSTER PASS 票卡樣式（只套視覺，不含圖鑑蒐集機制——那個是新功能，這輪沒做）
- ✅ 後台四頁（登入、預約查詢、名額管理、活動設定）也套上同一套視覺語言（設計包本身沒有畫後台，這部分是我延伸套用的）
- **暫緩的功能性項目**（业主已確認範圍不含）：底部三分頁列（預約／地圖／我的）、MONSTER PASS 圖鑑蒐集解鎖機制、怪獸驗證 CAPTCHA 小遊戲——這些是新頁面/新機制，不只是換皮

過程中發現並修正一個手機版排版問題：頁首在窄螢幕上文字沒有正確收縮，會把整頁撐寬。用瀏覽器實測 1920px 桌機與 406px 手機寬度，確認目前沒有任何橫向溢出。

尚待業主回覆／確認的項目：
- 場次人數上限可以設得比 5 還小嗎？（4-5 附帶問題）
- 後台需不需要「手動新增一筆預約」（電話預約用）？（4-5 附帶問題）
- 寄信服務要用 Resend（免費額度可能撐不住開賣尖峰）還是業主提議的 Gmail？討論中傾向 Resend，待業主確認網域 DNS 授權方式
- 主機／網域帳號歸屬（由誰申請、由誰付費）

---

## 一、前台使用者介面

### 預約流程
- [x] ✅ 選擇預約日期（月曆介面）— `StepCalendar.tsx`
- [x] ✅ 選擇場次與人數 — `StepSessions.tsx`
- [x] ✅ 填寫預約資料 — `StepForm.tsx`
- [x] ✅ **確認資料**（5-2 新增）— `StepConfirm.tsx`，填完表單後先顯示完整內容讓使用者再檢查一次，按「已確認資料無誤」才真的送出，並註明「如有問題可至展場詢問現場人員」
- [x] ✅ 確認送出 — 送出會實際打 API、寫入資料庫

### 預約開放時間
- [x] ✅ 10/1 起開放 10 月、10/20 起開放 11 月、11/20 起開放 12 月
  — 規則存在 `event.phaseOpenRules`（設定化，非寫死），月曆會依規則鎖住未開放月份。**已用真實資料庫＋今天的實際日期測試**：現在（9/9）查詢 10 月，所有天數正確顯示「未開放」。開放時間業主暫定 10/1 00:01（待與市府確認），目前規則是以「日」為單位判斷，還沒到分鐘級精度，等業主確認正式時間後可能需要調整精度
- [x] ✅ 場次開始前 15 分鐘自動停止該場次預約（業主須知 4-2）
  — 即時計算「現在時間」與「場次開始時間」，不需要排程改資料庫欄位，前台場次列表與訂票 API 都會檢查，**已實測**：距離開始 4 分鐘的場次會從清單消失、直接訂也會被擋下（距離 39 分鐘的場次正常可訂）
- [x] ✅ 展期結束後頁面全部關閉（業主須知 4-2）
  — 超過 `dateRangeEnd` 會顯示「本次活動已結束」畫面，不只是月曆變灰

### 預約填寫欄位
- [x] ✅ 姓名（必填）
- [x] ✅ 聯絡電話（必填）
- [x] ✅ 電子信箱（必填）
- [x] ✅ 人數（必填，1–5 人選擇器）
- [x] ✅ 預約日期（必填，從 Step1 帶入）
- [x] ✅ 選擇場次（必填，從 Step2 帶入）

### 預約限制
- [x] ✅ 每筆預約人數上限 5 人 — 前端選擇器 1–5、後端 zod 驗證雙重把關
- [x] ✅ 每場次名額上限 30 人，額滿自動停止開放
  — 資料庫層**單一交易原子扣減**（`UPDATE ... WHERE remaining >= headcount`），非只靠前端判斷，可正確防止超賣。**已實測驗證**：用真的 PostgreSQL（Neon）對同一場次送出 200 筆、後續再加測 500 筆併發請求，結果都是剛好賣完上限、其餘乾淨收到「已額滿」、資料庫剩餘名額與實際訂位筆數完全吻合，沒有超賣。過程中抓到並修好一個真問題——交易預設等待連線只有 2 秒，高併發時會直接丟出伺服器錯誤而非「已額滿」，已拉長等待時間修正（`src/app/api/bookings/route.ts`）
- [x] ✅ 場次選擇頁即時顯示各場次剩餘名額
- [x] ✅ 每筆預約限選一個場次
- [x] ~~每日每人限預約一次~~ **業主指示取消，已全面開放**（業主須知 4-1）
  — 原本用信箱／電話正規化＋資料庫唯一索引擋重複，已依業主決定移除，同一人現在可以重複預約
- [x] ✅ 每週一休館不開放預約選取
- [x] ✅ 不提供取消或修改預約功能（依規格刻意不做，改用送出前的確認資料頁面防呆，見上方 5-2）

### 預約確認機制
- [ ] ⛔ 送出後系統自動寄送確認信至預約者信箱
  — 尚未串接真正發信服務，`src/lib/mailer.ts` 目前只是 console.log 佔位。討論中決定改用 Resend（業主原提議用 Gmail，但免費 Gmail 帳號在開賣尖峰大量寄信容易被判定為垃圾郵件或觸發寄送限制，風險較高）
- [ ] ⛔ 確認信寄件者名稱與信箱
  — 待網域確定後設定
- [ ] ⚠️ 確認信內容（姓名、預約日期、場次、人數、入場注意事項）
  — 資料結構已備妥（送出成功後會組出完整的這些欄位），但因為信還沒真的寄出，內容尚未實際套版與送達

---

## 二、後台管理介面

### 帳號設定
- [x] ✅ 後台帳號一組，不分權限
  — `/admin/*` 與 `/api/admin/*` 已由 `src/middleware.ts` 統一擋下未登入請求；帳密存在資料庫（bcrypt 雜湊，非明文），登入用簽章 session cookie（7 天效期），`npm run prisma:seed` 會用 `.env` 的 `ADMIN_EMAIL`/`ADMIN_PASSWORD` 建立這組帳號。**已用真實資料庫測試**：正確密碼登入成功、拿到 cookie 後能查到真實預約資料、錯誤密碼正確擋下

### 預約資料查詢
- [x] ✅ 可依日期、場次篩選查詢
  — 日期與場次篩選都做好了，已用真實資料庫驗證過篩選結果正確
- [x] ✅ 顯示欄位：姓名、電話、信箱、人數、預約日期、場次、預約時間戳記
  — 全部欄位都顯示，時間戳記以 Asia/Taipei 時區格式化顯示

### 名額管理
- [x] ✅ 即時查看各場次剩餘名額（後台介面）
  — 新增 `/admin/sessions` 畫面，選日期即可看當天所有場次的上限／已預約／剩餘名額
- [x] ✅ 手動調整各場次人數上限
  — 已用真實資料庫測試邊界情況（第 3.5 節）：把上限調到低於已預約人數時，不影響已成立的預約，剩餘名額正確夾到 0；過程中抓到並修好一個真問題——原本用「舊剩餘名額 + 差值」計算，會在夾到 0 那次弄丟精確度，導致之後把上限調回去時剩餘名額算錯，已改成每次都直接用「上限 - 實際已預約人數」重新算，不會有累積誤差。**業主表示實務上不會用到調降這個功能**（4-5），固定人數就好，此功能保留但不特別強調
- [x] ✅ 手動開放或關閉特定場次
  — 已測試：關閉後前台再嘗試預約會被正確擋下（回傳「此場次已關閉」）

### 活動設定
- [x] ✅ 布告欄文字後台可自行編輯（業主須知 4-3）
  — 新增 `/admin/settings` 頁面，已測試修改後前台立即讀到新文字，未登入無法修改

### 資料匯出
- [x] ✅ 依日期區間匯出 Excel／CSV
  — 新增 `/admin/export` 頁面，匯出真正的 Excel（.xlsx，用 `exceljs`），已解壓縮實際比對過內部 XML 內容：中文姓名、電話開頭的 0、狀態欄位翻譯（confirmed→已確認）都正確，欄寬也設好不會有「####」顯示不下的問題
- [x] ✅ 單日或單場次匯出
  — 起訖日期設成同一天即為單日匯出；單日狀態下可另外指定單一場次，已測試篩選結果正確（只匯出該場次的預約）
- [x] ✅ 額外做了：記錄「誰於何時匯出」（`AdminExportLog`，PROJECT_SPEC.md 第 7 節建議事項），已驗證匯出時會正確寫入操作者帳號、範圍、筆數

### 通知功能
- [ ] ⛔ 後台手動發送通知信
- [ ] ⛔ 預約前一日自動寄送入場提醒信
  — 資料庫已預留 `reminderSentAt` 欄位（避免重複寄送用），但排程尚未實作

### 統計報表
- [ ] ⛔ 各日期／場次預約人次統計
- [ ] ⛔ 整體預約率追蹤
- [x] ~~手動標記到場狀態~~ **業主指示取消**（業主須知 4-6）
  — 原本已做好可點擊切換的到場狀態功能，業主決定現場改用人工計數器統計，後台不需要這個功能，已移除相關 UI 與 API

---

## 三、系統技術需求

- [x] ✅ 行動裝置版型支援（RWD）— 手機寬度優先設計
- [ ] ⚠️ 基本防機器人機制（CAPTCHA）
  — Honeypot 隱藏欄位已完成；後端 Cloudflare Turnstile 驗證流程已寫好，但**前端 Turnstile 小工具還沒實際掛到畫面上**，需要正式 Site Key 才能接上
- [ ] ⚠️ 資料安全儲存，符合個資保護原則
  — 個資同意勾選與時間戳記記錄已完成，同意聲明文字已與業主確認沿用；環境變數（金鑰等）沒有寫死在程式碼裡。但 HTTPS/TLS、資料庫靜態加密、Log 遮蔽個資等屬於**正式部署環境**才會生效的機制，本機雛形階段尚未套用
- [ ] ⚠️ 系統需穩定支援展期每日流量
  — 已用 `scripts/load-test-booking.ts` 對**單一場次**做過 200 及 500 併發搶名額測試並驗證不超賣、系統不會掛掉（見上方），這是最容易出事的「同一瞬間搶同一格」情境；**還沒測過的是**：多場次同時、長時間持續流量下的整體效能，也還沒有正式部署環境（Zeabur）可驗證真實網路延遲與環境下的表現。500 併發下部分請求要等到 10 幾秒才有回應，正式環境建議用付費資料庫方案改善
- [x] ✅ 介面語言：繁體中文

---

## 總結

| 區塊 | 已完成 | 部分完成 | 尚未開始 |
|---|---|---|---|
| 一、前台介面 | 18 | 1 | 2 |
| 二、後台介面 | 11 | 0 | 4 |
| 三、系統技術需求 | 2 | 3 | 0 |

**白話講**：核心的預約流程（含防呆確認頁）跟最重要的搶名額邏輯，都已經用真的資料庫做過高併發壓力測試驗證過，沒有超賣、系統不會掛掉。這輪業主回覆的須知也都已經對應改完程式碼：去重限制拿掉了、場次開始前 15 分鐘自動停止預約、展期結束會顯示明確的結束畫面、布告欄後台可以自己編輯、送出前多了一層確認資料防呆、到場狀態功能依指示移除。資料匯出這輪也做完並實測過，改成真正的 .xlsx（不是假裝成 Excel 的 CSV），中文、電話開頭 0、狀態翻譯都驗證過正確，還額外做了匯出紀錄。還沒做的集中在**通知信手動發送（含前一日提醒排程）、統計報表**跟**真正寄信**這幾塊，這些是接下來要補的部分。
