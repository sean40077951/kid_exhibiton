# 怪獸放電場｜線上預約系統 — 功能對照進度

依你提供的「預約系統需求規格」逐項對照目前雛形（`c:\Users\sean.wang.WENTAI\my project\kid_exhibiton`）的完成狀況。

圖例：✅ 已完成　⚠️ 部分完成（有做但不完整）　⛔ 尚未開始

---

## 一、前台使用者介面

### 預約流程
- [x] ✅ 選擇預約日期（月曆介面）— `StepCalendar.tsx`
- [x] ✅ 選擇場次與人數 — `StepSessions.tsx`
- [x] ✅ 填寫預約資料 — `StepForm.tsx`
- [x] ✅ 確認送出 — 送出會實際打 API、寫入資料庫

### 預約開放時間
- [x] ✅ 10/1 起開放 10 月、10/20 起開放 11 月、11/20 起開放 12 月
  — 規則存在 `event.phaseOpenRules`（設定化，非寫死），月曆會依規則鎖住未開放月份。**已用真實資料庫＋今天的實際日期測試**：現在（9/8）查詢 10 月，所有天數正確顯示「未開放」

### 預約填寫欄位
- [x] ✅ 姓名（必填）
- [x] ✅ 聯絡電話（必填）
- [x] ✅ 電子信箱（必填）
- [x] ✅ 人數（必填，1–5 人選擇器）
- [x] ✅ 預約日期（必填，從 Step1 帶入）
- [x] ✅ 選擇場次（必填，從 Step2 帶入）

### 預約限制
- [x] ✅ 每筆預約人數上限 5 人 — 前端選擇器 1–5、後端 zod 驗證雙重把關
- [x] ✅ 每場次名額上限 30 人，額滿自動停止開放
  — 資料庫層**單一交易原子扣減**（`UPDATE ... WHERE remaining >= headcount`），非只靠前端判斷，可正確防止超賣。**已實測驗證**：用真的 PostgreSQL（Neon）對同一場次送出 200 筆併發請求，結果剛好 30 筆成功、170 筆乾淨收到「已額滿」、資料庫剩餘名額與實際訂位筆數完全吻合，沒有超賣。過程中抓到並修好一個真問題——交易預設等待連線只有 2 秒，高併發時會直接丟出伺服器錯誤而非「已額滿」，已拉長等待時間修正（`src/app/api/bookings/route.ts`）
- [x] ✅ 場次選擇頁即時顯示各場次剩餘名額
- [x] ✅ 每筆預約限選一個場次
- [x] ✅ 每日每人限預約一次（以信箱或電話判斷）
  — 資料庫唯一索引 `(eventId, bookingDate, emailNormalized)` / `(..., phoneNormalized)`，電話與信箱皆正規化後比對。**已實測驗證**：同信箱不同場次會被擋下；同電話但輸入格式不同（如 `0911-222-333` vs `0911222333`）也正確被判定為同一人擋下；換成完全不同的人則正常成功
- [x] ✅ 每週一休館不開放預約選取
- [x] ✅ 不提供取消或修改預約功能（依規格刻意不做）

### 預約確認機制
- [ ] ⛔ 送出後系統自動寄送確認信至預約者信箱
  — 尚未串接真正發信服務，`src/lib/mailer.ts` 目前只是 console.log 佔位
- [ ] ⛔ 確認信寄件者名稱與信箱
  — 需業主提供，且待發信服務串接後才能設定
- [ ] ⚠️ 確認信內容（姓名、預約日期、場次、人數、入場注意事項）
  — 資料結構已備妥（送出成功後會組出完整的這些欄位），但因為信還沒真的寄出，內容尚未實際套版與送達

---

## 二、後台管理介面

### 帳號設定
- [x] ✅ 後台帳號一組，不分權限
  — `/admin/*` 與 `/api/admin/*` 已由 `src/middleware.ts` 統一擋下未登入請求；帳密存在資料庫（bcrypt 雜湊，非明文），登入用簽章 session cookie（7 天效期），`npm run prisma:seed` 會用 `.env` 的 `ADMIN_EMAIL`/`ADMIN_PASSWORD` 建立這組帳號。**已用真實資料庫測試**：正確密碼登入成功、拿到 cookie 後能查到真實預約資料、錯誤密碼正確擋下

### 預約資料查詢
- [ ] ⚠️ 可依日期、場次篩選查詢
  — 目前只做了「依日期」篩選，「依場次」篩選還沒做
- [ ] ⚠️ 顯示欄位：姓名、電話、信箱、人數、預約日期、場次、預約時間戳記
  — 姓名/電話/信箱/人數/日期/場次都有顯示，但畫面上**還沒把「預約時間戳記」欄位顯示出來**（API 有回傳，只是表格漏加這欄）

### 名額管理
- [ ] ⛔ 即時查看各場次剩餘名額（後台介面）
  — 資料庫有即時 `remaining` 欄位、前台會用到，但**後台還沒有一個畫面**專門給管理者總覽查看
- [ ] ⛔ 手動調整各場次人數上限
- [ ] ⛔ 手動開放或關閉特定場次
  — 資料庫已有 `isOpen` 欄位可支援，但沒有後台介面可以操作它

### 資料匯出
- [ ] ⛔ 依日期區間匯出 Excel／CSV
- [ ] ⛔ 單日或單場次匯出

### 通知功能
- [ ] ⛔ 後台手動發送通知信
- [ ] ⛔ 預約前一日自動寄送入場提醒信
  — 資料庫已預留 `reminderSentAt` 欄位（避免重複寄送用），但排程尚未實作

### 統計報表
- [ ] ⛔ 各日期／場次預約人次統計
- [ ] ⛔ 整體預約率追蹤
- [ ] ⚠️ 手動標記到場狀態
  — 資料庫有 `checkedIn` 欄位、後台表格會顯示目前狀態，但**還不能用滑鼠點擊切換**，只是唯讀顯示

---

## 三、系統技術需求

- [x] ✅ 行動裝置版型支援（RWD）— 手機寬度優先設計
- [ ] ⚠️ 基本防機器人機制（CAPTCHA）
  — Honeypot 隱藏欄位已完成；後端 Cloudflare Turnstile 驗證流程已寫好，但**前端 Turnstile 小工具還沒實際掛到畫面上**，需要正式 Site Key 才能接上
- [ ] ⚠️ 資料安全儲存，符合個資保護原則
  — 個資同意勾選與時間戳記記錄已完成；環境變數（金鑰等）沒有寫死在程式碼裡。但 HTTPS/TLS、資料庫靜態加密、Log 遮蔽個資等屬於**正式部署環境**才會生效的機制，本機雛形階段尚未套用
- [ ] ⚠️ 系統需穩定支援展期每日流量
  — 已用 `scripts/load-test-booking.ts` 對**單一場次**做過 200 併發搶名額測試並驗證不超賣（見上方），但這只證明了最容易出事的「同一瞬間搶同一格」情境；**還沒測過的是**：多場次同時、長時間持續流量下的整體效能與穩定度，也還沒有正式部署環境（Zeabur）可驗證真實網路延遲與環境下的表現
- [x] ✅ 介面語言：繁體中文

---

## 總結

| 區塊 | 已完成 | 部分完成 | 尚未開始 |
|---|---|---|---|
| 一、前台介面 | 17 | 1 | 2 |
| 二、後台介面 | 1 | 3 | 7 |
| 三、系統技術需求 | 2 | 3 | 0 |

**白話講**：使用者從打開網站到「預約成功、拿到編號」這一整條路，包含最容易出包的搶名額和防重複預約，不只邏輯做對了，還**實際用真的資料庫做過 200 人同時搶同一場次的壓力測試**，證實不會超賣、也在過程中修好一個真的會讓使用者看到伺服器錯誤的問題；還沒做的主要集中在**後台管理**（名額調整、匯出、統計）跟**真正寄信**這兩塊，這些是雛形之後要接續補上的部分。
