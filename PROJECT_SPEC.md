# 怪獸放電場｜兒藝節秋季展期 線上預約系統
## 專案規格文件（供 AI Coding 開發使用）

> 這份文件給 AI coding 工具（Claude Code / Cursor 等）閱讀，請嚴格依照本規格實作。
> 有不確定或規格未涵蓋之處，請先詢問，不要自行假設。

---

## 0. 專案一句話說明

一個**免費、無會員、不可取消改期**的活動場次線上預約系統。使用者選日期→選場次與人數→填資料→送出，取得預約編號與確認信。後台可管理場次名額、查詢匯出名單、發送通知。系統設計為**引擎＋皮**架構，未來可套用不同主題重複用於其他展會。

---

## 1. 技術棧（已定案）

| 項目 | 選擇 |
|---|---|
| 部署平台 | **Zeabur**（程式＋PostgreSQL＋排程皆在同平台） |
| 資料庫 | **PostgreSQL**（務必用支援交易的關聯式資料庫，不可用無交易保證的方案） |
| CDN / 防護 | Cloudflare（掛在最前面） |
| 發信服務 | 專業發信服務（如 Resend / Amazon SES），**不可用主機內建 mail()**，需設定 SPF/DKIM/DMARC |
| CAPTCHA | Cloudflare Turnstile（非 reCAPTCHA） |
| 語言/框架 | 待定（Laravel + PostgreSQL 或 Next.js + Prisma + PostgreSQL 皆可，擇一即可，本文件邏輯不因框架而變） |
| 版控 | Git，接 GitHub，Zeabur 從 GitHub 自動部署 |
| 容器化 | Docker（本機開發用，正式環境視 Zeabur 平台機制） |
| 時區 | **Asia/Taipei**（全系統，包含排程、日期判斷、時間戳記顯示） |
| 介面語言 | 繁體中文 |

**本機開發**：用 Docker 跑 PostgreSQL（或直接安裝 Postgres / 用 Neon 免費雲端皆可），程式在本機執行，連線字串指向本機或雲端 Postgres。開發環境與正式環境使用同一套 migration 與程式碼，僅環境變數不同。

---

## 2. 核心資料模型

```
events（展會設定，每檔展會一列，未來換展期＝新增一列，不改程式碼）
├── id
├── name                      -- 展會名稱（如：怪獸放電場）
├── theme_settings (JSON)     -- 顏色、字型、logo 等視覺設定
├── date_range_start / end    -- 展期區間 (10/10–12/27)
├── closed_weekday            -- 休館日（週一 = 1）
├── phase_open_rules (JSON)   -- 分階段開放規則，見第 4 節
├── banner_text                -- 首頁佈告欄文字（後台可編輯）
├── notice_text                -- 入場注意事項文字（後台可編輯）
├── email_template             -- 確認信模板（後台可編輯）
├── consent_text               -- 個資同意聲明文字（【由業主提供內容，系統僅負責顯示與記錄同意，勿自行編寫法律文字】）
└── data_retention_days        -- 資料保存天數，到期後可銷毀

session_templates（每日場次時間樣板）
├── id
├── event_id
├── time_slots (JSON)          -- 例：["10:00","10:35","11:10",...]
└── capacity_per_slot          -- 單場人數上限（預設 30）

sessions（實際場次，由樣板 × 日期展開）
├── id
├── event_id
├── date
├── time_slot
├── capacity                   -- 該場次人數上限（後台可個別調整）
├── remaining                  -- 剩餘名額（原子扣減見第 3 節）
└── is_open                    -- 是否開放（後台可手動關閉，如突發休館）

bookings（預約紀錄）
├── id
├── event_id / session_id
├── booking_code                -- 預約編號，格式如 aaa10310001，見第 5 節
├── name / phone / email
├── phone_normalized / email_normalized  -- 正規化後欄位，用於去重比對
├── headcount                   -- 人數（1–5）
├── consent_given_at            -- 個資同意時間戳
├── consent_version              -- 同意條款版本（若條款會變動）
├── status                      -- pending / confirmed / cancelled(僅後台特殊情況)
├── checked_in                  -- 到場狀態（後台手動標記）
├── created_at

admins（後台帳號）
├── id / email / password_hash   -- 單一角色，不分權限（依規格）
```

**唯一索引（防重複預約，資料庫層強制，不可只靠前端驗證）：**
```sql
UNIQUE(event_id, booking_date, email_normalized)
UNIQUE(event_id, booking_date, phone_normalized)
```
`booking_date` 為使用者選擇的預約日期（不是 created_at），因為規則是「每日每人限一次」。

---

## 3. 核心技術難點（必須正確實作，優先序由高到低）

### 3.1 名額原子扣減（最高優先，全系統最重要的一段邏輯）

**問題**：多人同時搶同一場次最後名額時，若採「先讀剩餘→判斷→寫入」會導致超賣。

**正確做法**：單一原子 UPDATE + 交易，在同一 transaction 內完成名額檢查與扣減：

```sql
BEGIN;
UPDATE sessions
SET remaining = remaining - :headcount
WHERE id = :session_id AND remaining >= :headcount;
-- 若影響列數 = 0，代表名額不足，ROLLBACK 並回傳「已額滿」
-- 若影響列數 = 1，繼續於同一 transaction 內 INSERT INTO bookings
COMMIT;
```

- 絕對不可以在應用層先 `SELECT remaining` 判斷夠不夠、再另外 `UPDATE`——這中間有競態空隙。
- 取消預約（僅後台特殊情況使用）需要對稱地把名額加回去，同樣要交易保護。
- 上線前需**壓力測試**：模擬多筆請求同時搶同一場次最後名額，驗證（a）不超賣（b）回應時間可接受。可用 k6 或 Apache Bench。

### 3.2 去重驗證

- **正規化**後再比對：電話統一格式（去除 `-`、統一 `+886`/`0` 開頭）；Email 轉小寫。
- Email **或**電話任一命中即視為重複，需兩個唯一索引都檢查（見第 2 節）。
- 資料庫唯一索引是最後防線，前端驗證只是提升使用者體驗，**不能省略資料庫層**。
- 同一人快速連點兩次送出的競態，交由資料庫唯一索引擋下（INSERT 失敗則回傳友善錯誤訊息）。

### 3.3 時區與排程可靠性

- 系統所有時間運算使用 **Asia/Taipei**，伺服器與資料庫時區設定需一致確認，不可讓「今天」的邊界因 UTC 而位移。
- 「預約前一日自動寄送提醒信」為**每日排程**，需確保：
  - 服務重啟不漏寄、不重複寄（可用「今日已寄送」標記欄位防重複）。
  - 部署到 Zeabur 前，需確認該平台的排程機制如何設定與觸發，並在上線前實測。

### 3.4 確認信與提醒信送達率

- 使用專業發信服務（非主機內建 mail()），設定 SPF/DKIM/DMARC 避免進垃圾信匣。
- 發信走**佇列（queue）非同步**處理，避免拖慢使用者送出預約時的回應速度。
- 確認信內容需包含：姓名、預約日期、場次、人數、預約編號、入場注意事項。

### 3.5 後台即時異動的邊界情況（需明確定義，勿放任程式產生未定義狀態）

- 若後台把場次上限改到低於「目前已預約人數」，系統應：**不影響已成立的預約**，僅阻擋後續新預約超過已預約人數。
- 若後台關閉一個已有預約的場次，需有明確流程（此為業務規則，若規格未明訂，開發前應詢問，不要自行假設）。

### 3.6 CAPTCHA（Cloudflare Turnstile）

前端與後端都要做，缺一不可：

```html
<script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>
<div class="cf-turnstile" data-sitekey="YOUR_SITE_KEY"></div>
```

送出預約時，後端必須向 Cloudflare 驗證 token 後才可寫入資料庫：

```js
const r = await fetch('https://challenges.cloudflare.com/turnstile/v0/siteverify', {
  method: 'POST',
  headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
  body: new URLSearchParams({
    secret: process.env.TURNSTILE_SECRET, // 環境變數，勿寫死於程式碼
    response: token,
    remoteip: req.ip
  })
});
const data = await r.json();
if (!data.success) { /* 拒絕，回傳錯誤，不寫入資料庫 */ }
```

另外搭配：
- **Honeypot 欄位**：表單中放一個對使用者隱藏（CSS 隱藏，非 `display:none` 這種容易被機器人偵測略過的方式）、機器人容易誤填的欄位，一旦有值即判定為機器人並拒絕。
- **Rate limiting**：同 IP／同來源短時間內大量送出請求時限制頻率。

> 註：CAPTCHA 只能防機器人，無法防止真人使用假資料人工灌水佔位；此為無登入門檻系統的先天限制，非技術問題。

---

## 4. 分階段開放預約排程

```
10/1  起開放「10 月」場次預約
10/20 起開放「11 月」場次預約
11/20 起開放「12 月」場次預約
```

實作建議：`events.phase_open_rules` 存放 `[{open_date, applies_to_month}]` 陣列，系統依當下日期（Asia/Taipei）判斷哪些月份的場次可被預約，未開放月份的日期在前台月曆應顯示為不可選。**此規則需可透過設定調整，不要寫死在程式邏輯中**（未來換展期时間會不同）。

---

## 5. 預約編號規則

格式範例：`aaa10310001`（依業主提供之確切規則為準，若尚未取得規則細節，開發前需再確認）。建議做法：可設定的前綴（如展會代碼）＋日期或流水號組成，需保證**同一展會期間不重複**。

---

## 6. 前台四步驟流程

```
Step 1 選擇日期（月曆介面）
  - 顯示範圍：event.date_range_start ~ end
  - 週一（或 event.closed_weekday）不可選
  - 未開放月份（依第 4 節規則）不可選
  - 已額滿日期需視覺標示

Step 2 選擇場次與人數
  - 帶入 Step 1 選定日期
  - 列出當日場次，各場次即時顯示剩餘名額（remaining）
  - 額滿場次不可選 / 顯示「已滿」
  - 人數選擇 1–5（headcount）
  - 顯示提示：「3 歲以下兒童需至少 1 位成人同行」

Step 3 填寫預約資料
  - 唯讀顯示：日期／場次／人數
  - 必填：姓名、聯絡電話、電子信箱
  - 顯示提示：「不提供取消與更改功能，請確認後再送出」「提前 10 分鐘到場報到，逾時不候」
  - 個資同意聲明（event.consent_text，勾選才可送出，需記錄 consent_given_at）
  - CAPTCHA（Turnstile）
  - 送出時：後端驗證 CAPTCHA → 檢查去重 → 交易內原子扣減名額 + 建立預約 → 觸發確認信（非同步）

Step 4 預約完成
  - 顯示預約編號、姓名、日期、場次、人數
  - 提示確認信已寄出
  - 顯示入場注意事項
  - 回首頁按鈕
```

RWD 必須支援手機操作（規格明訂主要使用情境為手機）。

---

## 7. 後台需求

| 功能 | 說明 |
|---|---|
| 帳號 | 單一組帳號，不分權限（依規格），建議強密碼＋可選兩步驟驗證 |
| 預約查詢 | 依日期、場次篩選；顯示姓名、電話、信箱、人數、預約日期、場次、時間戳記 |
| 名額管理 | 即時查看各場次剩餘名額；手動調整場次人數上限；手動開關特定場次 |
| 資料匯出 | 依日期區間匯出 Excel／CSV；可單日或單場次匯出；**下載須經 HTTPS 後台介面，禁止以 Email 明文寄送名單**；建議記錄「誰於何時匯出」 |
| 通知功能 | 手動發送通知信（活動異動、場次調整用）；設置預約前一日自動提醒信（見 3.3） |
| 統計報表 | 各日期/場次預約人次統計；整體預約率；支援手動標記到場狀態（checked_in） |
| 文字維護 | 佈告欄、注意事項文字可後台編輯（對應 event.banner_text / notice_text） |

---

## 8. 明確排除項目（不要實作，避免過度開發）

金流／付款、會員登入與註冊、取消或修改預約功能、員工排班與業績、CRM 顧客檔案與黑名單、多門市管理、多語系、PWA、滿意度問卷、多角色權限（RBAC）。**若開發中發現任何規格似乎暗示需要以上功能，請先詢問而非自行加入。**

---

## 9. 個資與安全（技術實作範圍界定）

> 重要分際：**告知同意聲明的文字內容由業主提供並負責其合法性，不由開發端撰寫。開發端負責的是「機制」，不是「內容」。**

| 項目 | 開發端負責的機制 |
|---|---|
| 同意聲明 | 提供勾選元件（不勾不可送出）、原文顯示業主提供之文字、記錄同意時間戳與條款版本 |
| 傳輸安全 | 全站 HTTPS/TLS |
| 儲存安全 | 資料庫靜態加密；DB 帳號最小權限；密鑰放環境變數，不進 Git／不進原始碼 |
| 匯出管控 | 僅能經後台 HTTPS 下載；不寄明文名單；記錄匯出行為 |
| 資料保存期限 | `event.data_retention_days` 到期後可（自動或後台一鍵）刪除，含備份一併處理 |
| 測試資料 | 開發與測試環境不可使用真實個資，一律使用假資料 |
| Log 紀錄 | 記錄後台登入與匯出等操作，但**不可將完整個資寫入應用日誌** |

---

## 10. 部署（Zeabur）

- GitHub repo → Zeabur Project 自動部署。
- 同專案內加 PostgreSQL 服務，取得連線字串設定於環境變數。
- 環境變數需包含（但不限於）：`DATABASE_URL`、發信服務金鑰、`TURNSTILE_SECRET`、`TURNSTILE_SITE_KEY`。皆不可寫死於程式碼或提交進版控。
- 確認 Zeabur 排程機制可正確觸發「前一日提醒信」的每日排程，上線前實測。
- 綁定網域，前面掛 Cloudflare。
- 確認資料庫有自動備份機制。

---

## 11. 開發時的原則提醒（給 AI coding 工具）

1. **設定化優先**：任何「這檔展會會變、下一檔可能不同」的內容（主題色、場次時間、日期區間、文案、信件模板、分階段開放規則）一律存資料庫或設定，不寫死在程式碼或條件判斷中。
2. **交易與唯一索引是底線**：涉及名額與去重的邏輯，一律在資料庫層有對應保護，不可只靠應用層邏輯或前端驗證。
3. **規格未涵蓋的行為，先問不要猜**：尤其是後台異動的邊界情況（第 3.5 節）、預約編號確切規則（第 5 節）、確認信寄件者資訊等，規格文件目前未完全確認，動工前應向使用者確認。
4. **不做規格外的功能**：見第 8 節排除清單，即使該功能看似「順手就能加」也不要加。
